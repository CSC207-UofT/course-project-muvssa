<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fitappa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat">
        <activity android:name=".workout.workout_log.WorkoutLogActivity" />
        <activity android:name=".profile.DashboardActivity" />
        <activity android:name=".authentication.LoginActivity" />
        <activity android:name=".workout.workout_template.ViewWorkoutActivity" />
        <activity android:name=".routine.AddRoutineActivity" />
        <activity android:name=".workout.track_workout.TrackWorkoutActivity" />
        <activity android:name=".workout.workout_template.AddWorkoutActivity" />
        <activity android:name=".exercise.exercise_template.AddExerciseActivity" />
        <activity android:name=".routine.StartWorkoutFromRoutinesActivity" />
        <activity android:name=".profile.SetupActivity" />
        <activity
            android:name=".authentication.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".authentication.SignUpActivity"
            android:exported="true" />
        <activity
            android:name=".profile.ViewProfileActivity"
            android:exported="true" />
    </application>

</manifest>