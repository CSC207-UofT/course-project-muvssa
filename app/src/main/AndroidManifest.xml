<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fitappa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat">
        <activity android:name=".View.DashboardActivity" />
        <activity android:name=".View.LoginActivity" />
        <activity android:name=".View.ViewWorkoutActivity" />
        <activity android:name=".View.ViewRoutineActivity" />
        <activity android:name=".View.AddRoutineActivity" />
        <activity android:name=".View.TrackWorkoutActivity" />
        <activity android:name=".View.AddWorkoutActivity" />
        <activity android:name=".View.AddExerciseActivity" />
        <activity android:name=".View.ViewRoutinesActivity" />
        <activity
            android:name=".View.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".View.SignUpActivity"
            android:exported="true" />
        <activity
            android:name=".View.ViewProfileActivity"
            android:exported="true" />
    </application>

</manifest>