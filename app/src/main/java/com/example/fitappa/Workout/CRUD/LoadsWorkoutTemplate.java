package com.example.fitappa.Workout.CRUD;

import com.example.fitappa.Workout.Core.WorkoutTemplate;

/**
 * This interface ensures that the specific workout template loads
 *
 * Methods in this class load a specific workout template
 *
 * Documentation specifies what the methods do
 *
 * @author Abdullah
 *
 * @since 0.2
 */


public interface LoadsWorkoutTemplate {
    void loadWorkoutTemplate(WorkoutTemplate workoutTemplate);
}
